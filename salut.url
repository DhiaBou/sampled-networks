absolute,

from the neural networks,

use the package for the sampled network, use that as input, get their weights, and so on,

test with theirss first, the algorithm works, 

approximate the network

every five epochs during dam training we use the sampled networks and then see how the weights changes, and what points are chosen

interpret the adam training, using our problem

